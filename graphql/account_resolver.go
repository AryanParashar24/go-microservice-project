// Now this will have our order query
// Orders
package main

type orderResolver struct {
	server *Server // this will have the server with the name Server as a field which will be
}

// as we can notice above that earlier at first we defined and reserved onnly orders to be placed and defined here in the account_resolver.go thus, we are here defining about the Orders query
// func (r *accountResolver) Orders(ctx context.Context, obj *Account)([]*Orders, err){

// }
